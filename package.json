{
  "name": "tarrot-service",
  "version": "0.1.0",
  "description": "A mystic game-like tarot card service powered by Phaser and Cloudflare Workers AI.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "prebuild": "python3 -c \"from pathlib import Path; p=Path('src/game/scenes/QuestionScene.ts'); s=p.read_text(); s=s.replace('button.label.setText(choice.label).setVisible(true);\\n      button.hit.setInteractive({ useHandCursor: true });', 'button.label.setText(choice.label).setPosition(x, y).setVisible(true);\\n      button.hit.setPosition(x, y);\\n      button.hit.setInteractive({ useHandCursor: true });'); p.write_text(s)\"",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy worker/entry.ts"
  },
  "dependencies": {
    "phaser": "^3.86.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "vite": "latest",
    "wrangler": "latest"
  }
}